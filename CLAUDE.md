# CLAUDE.md

กฎของ repo นี้ (คอนโซล AI Office — Vue 3 + ant-design-vue) ใช้กับทุกคนที่แก้โค้ด รวมถึง AI ช่วยเขียนโค้ด

## 1. แก้หรือเพิ่มอะไร ต้องเช็กคู่มือทุกครั้ง

- คู่มือคือไฟล์ Markdown ใน repo `ai-office-backend` → `docs/guide.md` (แหล่งเดียว) — หน้า `/guide` ของ repo นี้ (`src/views/guide/GuideView.vue`) ดึงผ่าน API ไปแสดง และผู้ช่วย AI ในคอนโซลใช้ตอบวิธีใช้
- ทุกงานที่เปลี่ยนสิ่งที่ผู้ใช้เห็นหรือทำ (เมนู ปุ่ม ช่องกรอก ขั้นตอน สิทธิ์ ข้อความ error พฤติกรรมของ widget/แชท) — รวมถึงงานฝั่ง `ai-office-backend` ที่ส่งผลถึงหน้าจอ — ให้เปิดคู่มืออ่านหัวข้อที่เกี่ยวข้อง
- ไม่ตรงกับของจริง → แก้คู่มือให้ตรงในงานเดียวกัน (รวมหัวข้อ "แก้ปัญหาที่พบบ่อย" และป้ายสิทธิ์ของหัวข้อ)
- ฟีเจอร์ใหม่ที่ผู้ใช้ต้องรู้ → เพิ่มหัวข้อหรือข้อย่อยในคู่มือ
- ตอนส่งงาน / เปิด PR ให้บอกว่าแก้คู่มือตรงไหน หรือบอกว่าเช็กแล้วไม่ต้องแก้

## 2. UI ที่มีอยู่แล้ว ให้ใช้ของเดิม ห้ามสร้างแบบใหม่

หน้าตาและพฤติกรรมต้องเหมือนกันทุกหน้า — ก่อนทำ UI ให้หาของที่ใช้อยู่ใน `src/components`, `src/utils`, `src/style.css` ก่อนเสมอ

| ต้องการ | ใช้ |
|---|---|
| เลือก domain | `components/DomainSelect.vue` |
| dropdown ที่ตัวเลือกมาจาก API | `show-search :filter-option="matchOption"` (`utils/selectSearch.ts`) |
| ช่วงวันที่ | `components/DateRangeFilter.vue` |
| แถบตัวกรอง | class กลาง `.filter-grid` `.f-label` `.filter-foot` `.f-count` `.span-2` ใน `style.css` · กรองทันทีไม่มีปุ่มค้นหา · loading ของปุ่มรีเฟรชแยกจากตาราง |
| คำอธิบายช่องกรอก | ไอคอน ! `components/InfoTip.vue` (ไม่เขียนข้อความยาวใต้ช่อง) |
| รหัส 2FA | `components/PinInput.vue` |
| การ์ดข้อมูลในแชท | `components/ChatCard.vue` |
| กราฟ | `components/charts/*` + สีจาก `utils/chartPalette.ts` |
| ไอคอนเมนู | `components/NavIcon.vue` |
| หัวหน้า | `.page-head` (h1 + `.sub`) |
| การ์ด | `<a-card size="small" class="tidy">` |
| ป้ายสิทธิ์ / เมนู | `utils/permissionGroups.ts` |
| ป้ายประวัติการทำงาน | `utils/auditLabels.ts` |

- ตัวอย่างใน placeholder ใช้ชื่อสมมติ (`example-...`) ห้ามใช้ชื่อลูกค้าหรือระบบจริง
- ของเดิมไม่พอ → ขยายตัวเดิม (เพิ่ม prop) แทนการทำตัวใหม่
- จำเป็นต้องมีแบบใหม่จริง → อธิบายว่าทำไมของเดิมใช้ไม่ได้ และทำเป็นของกลางที่หน้าอื่นใช้ต่อได้ แล้วเพิ่มลงตารางข้างบน
